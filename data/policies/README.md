# Policy corpus (YOUR task — do not generate these)

Write 3 docs like real store pages, 300–500 words each:
  returns.md   — windows (30d standard, 7d clearance/final sale), condition rules, exceptions
  shipping.md  — methods, timeframes, delay handling, lost package process
  refunds.md   — method + timeline, partial refunds, thresholds requiring review

Include specific numbers the agent can cite. Conflicts between docs are
features, not bugs — they make retrieval quality visible.
